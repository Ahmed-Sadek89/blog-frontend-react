export type userPayload = {
    username: string,
    email: string,
    password: string,
    image: string,
}

export type userInfo = {
    id: number,
    username: string,
    email: string,
    password: string,
    image: string,
    created_at: string
}